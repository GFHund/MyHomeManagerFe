import { FormRadioGroupDirective } from './form-radio-group.directive';

describe('FormRadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new FormRadioGroupDirective();
    expect(directive).toBeTruthy();
  });
});
